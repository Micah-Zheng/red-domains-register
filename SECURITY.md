# 安全报告

发现漏洞请不要开公开 issue，用 GitHub 的私密报告通道：
Security → Report a vulnerability。

## 特别关注的方向

这个项目把 CI 凭据、DNS 写权限和任意人可提交的 PR 放在同一个仓库里，所以以下几类问题优先级最高：

- **凭据越界**：任何能让 PR 内容在持有 `CF_API_TOKEN` 的上下文里执行的路径。三段流水线的边界（阶段①无凭据可检出 / 阶段②有 AI 凭据不检出 / 阶段③仅在已合并的 main）是核心不变式，`tests/workflow-boundary.e2e.mjs` 守着它。
- **提示注入提权**：`description` 是提交者完全可控的自由文本。设计上 AI 只能把结论从放行降为人工，不能反向提权。若你找到让 AI 输出直接导致自动合并的路径，那是高危。
- **所有权挑战绕过**：能在不控制解析目标的情况下通过挑战，即可劫持子域名指向。
- **校验绕过**：让保留词、短前缀或他人已占用的前缀通过校验。

## 不算漏洞

- 用户自己的站点内容有问题——那是该站点的事，滥用请走举报 issue
- Cloudflare 免费 Universal SSL 不覆盖多层子域名——这是已知的档位限制，README 里写明了
